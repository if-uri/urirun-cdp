# Example environment file for urirun-cdp development.
# Copy to .env (git-ignored) and fill in values as needed.

# Optional: override Python interpreter used by the Makefile.
# PYTHON=python3.11

# Optional: Chrome/Chromium binary used by CDP-based tests.
# CHROME_BINARY=
